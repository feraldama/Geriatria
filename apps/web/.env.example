# ─── Frontend (apps/web) ───────────────────────────────────────────────────
# URL interna de la API a la que Next reenvía /api/v1/* (proxy server-side).
# En desarrollo apunta al Express local. El navegador nunca la ve directamente.
API_INTERNAL_URL="http://localhost:3027"
